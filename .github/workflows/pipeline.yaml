name: TestPipelineAction action
on:
  push:

jobs:
  Test_Partitation:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          ref: 'refs/*/*'
      - run: |
          ls -a
          npm install
          npm start

